# Aventura Telegram RPG Bot

Локальный MVP для асинхронной текстовой RPG в Telegram.

Источник истины один: локальная SQLite-база `data/aventura.sqlite`.
Игроки не ведут отдельные листы вручную, а смотрят актуальные данные через команды бота.

Архитектура заложена под несколько режимов игры:

- основной PvE-бот с миссиями;
- будущий PvP-бот для дуэлей и турниров;
- будущий режим фермы гильдии для развития Каррок Манора и инфраструктуры Авентуры.

Все режимы используют одну таблицу героев `characters`, но хранят собственное состояние в отдельных таблицах.

Игровой цикл:

1. Главный мастер готовит `turn_seed.yaml` или просто описывает тему хода.
2. Бот сохраняет seed в `data/seeds/` и готовит workbench в `data/workbench/`.
3. Codex генерирует готовый `turn.yaml`, сверяясь с энциклопедией, базой персонажей и указаниями мастера.
4. Codex добавляет в `turn.yaml` `turn.art.prompt` для общей иллюстрации хода.
5. Если мастер хочет картинку на старте, он генерирует арт и отправляет его боту с подписью `/turn_art`.
6. Админ загружает боту `turn.yaml`.
7. Бот открывает ход, создает миссии и, если арт был загружен, публикует картинку перед миссиями.
8. Бот рассылает список миссий всем игрокам, которые уже нажали `/start`.
9. Игроки выбирают миссию и отправляют действие.
10. В `deadline` бот сам закрывает ход, сохраняет экспорт в `data/exports/`, присылает JSON админу и готовит `data/workbench/turn_<id>/`.
11. Главный мастер при необходимости кладет в workbench `resolution_notes.yaml`.
12. Codex обрабатывает экспорт и `resolution_notes`, затем готовит `result.json`.
13. Codex или мастер кладет `result.json` в `data/imports/pending/` либо админ загружает файл боту.
14. Бот делает бэкап базы, импортирует результат, обновляет городскую хронику и публикует итоги игрокам.

## Мастерские указания

Генерация миссий поддерживает несколько уровней детализации:

- если мастер ничего не уточнил, Codex генерирует миссии сам;
- если мастер указал тему, Codex ориентируется на тему;
- если мастер дал наброски миссий, Codex сохраняет их и дополняет недостающее;
- если мастер указал конкретную сложность или награды, Codex берет их в работу и дополняет только недостающие поля.

При обработке хода мастер может приложить `resolution_notes.yaml`.
Эти комментарии имеют высший приоритет: можно указать, кому дать или не дать награду, какие события обязательны, какие последствия нужно сохранить.

## Установка

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env
```

В `.env` нужно заполнить:

```text
TELEGRAM_BOT_TOKEN=токен от BotFather
ADMIN_TELEGRAM_IDS=твой_numeric_telegram_id
TANELLORN_MINI_APP_ENABLED=false
TANELLORN_MINI_APP_ADMIN_ONLY=true
TANELLORN_MINI_APP_URL=
MISSION_UI_MODE=legacy
```

## Запуск

```bash
python -m aventura_bot.bot
```

Бот работает через long polling, поэтому публичный сервер и webhook не нужны.

## Mini App Танелорн

Mini App является интерфейсом к тем же ручным PvE-миссиям и существующим игровым сервисам. На карте уже доступны:

- выбор или смена миссии, отправка и замена хода;
- лист героя, последний результат и roster гильдии;
- лавка, продажа разрешенных активов, выкуп и платный отдых в таверне;
- алхимическая мастерская с текущим крафтом;
- аукцион как витрина существующих лотов игроков без механики ставок.

Mini App не меняет команды `/missions`, `/join`, `/action` и pipeline `turn.yaml -> export.json -> result.json`. Telegram-команды остаются fallback.

Режимы `MISSION_UI_MODE`:

- `legacy` - `/missions` показывает только прежний Telegram-список;
- `both` - `/missions` сохраняет список и добавляет кнопку `Открыть Танелорн`;
- `miniapp` - доступному пользователю `/missions` показывает кнопку карты вместо длинного списка, но `/join` и `/action` продолжают работать.

По умолчанию `TANELLORN_MINI_APP_ENABLED=false`, поэтому интерфейс бота остается прежним. На этапе разработки настрой `TANELLORN_MINI_APP_ADMIN_ONLY=true`: кнопка будет показана только пользователям из `ADMIN_TELEGRAM_IDS`, а API карты проверит подписанные Telegram `initData` или подписанный ботом URL admin-кнопки для клиентов Telegram, которые не передают usable `initData`. Admin-ссылка дает доступ к игровым действиям, действует 24 часа и не должна пересылаться. После истечения срока открой свежую кнопку через `/missions`.

Отдельный локальный запуск web-сервиса:

```bash
python -m aventura_bot.web
```

На Railway Mini App запускается вместе с long polling ботом в одном service:

```bash
python -m aventura_bot.runtime
```

Так бот и карта используют один существующий SQLite volume и одни сервисные правила. `Dockerfile` уже запускает этот совместный runtime; при выключенном флаге web-маршруты отвечают `404`, а старый Telegram-интерфейс продолжает работать.

При локальном просмотре в обычном браузере можно временно использовать `TANELLORN_MINI_APP_ADMIN_ONLY=false`; для admin-only проверки страницу следует открывать кнопкой внутри Telegram. План миграции описан в `docs/tanellorn_mini_app_plan.md`.

## Обязательная Проверка Перед Выкатом

Перед каждым `git push`, который должен попасть в Railway, обязательно запускай:

```bash
./scripts/predeploy_check.sh
```

Этот шаг обязателен для любых правок бота и связанных сервисов. Он:

- показывает список измененных файлов;
- прогоняет `py_compile` по главным Python-модулям;
- проверяет импорт `aventura_bot.bot`, чтобы не пропустить сломанный импорт или незакоммиченный связанный файл.

Минимальная безопасная процедура выката теперь такая:

1. Внести правки локально.
2. Запустить `./scripts/predeploy_check.sh`.
3. Если проверка зеленая, сделать `git add`, `git commit`, `git push`.
4. После деплоя в Railway проверить в Telegram:
   - `/start`
   - `/profile`
   - `Герой`
   - `Миссии`
   - `Мой ход`
   - `Лавка`
   - `Крафт`

Если `predeploy_check.sh` падает, выкатывать правки нельзя, пока причина не исправлена.

## Первый ручной тест

1. Напиши боту `/start`.
2. Создай персонажа:

```text
/create_character Рион | мужской | человек | Молодой авантюрист в сером плаще, привыкший замечать мелкие трещины в людях и вещах. | сила=6 ловкость=6 интеллект=5 харизма=4 восприятие=6 удача=3 | Искра стекла | кинжал, плащ, мел
```
3. При желании сгенерируй картинку по `turn.art.prompt` из `examples/turn.sample.yaml` и отправь ее боту с подписью `/turn_art`.
4. Загрузи файл `examples/turn.sample.yaml` в чат с ботом.
5. Выбери миссию: `/join 1`.
6. При желании запусти крафт кнопкой `Крафт` или командой `/craft`: выбери основу, материал и подтверди.
7. Отправь действие, можно с абзацами:

```text
/action Рион осматривает мастерскую и ищет следы зачарованного стекла.

Он не трогает зеркала руками, а отмечает мелом те, где отражение запаздывает.
```

7. Вызови `/export_turn`.
8. Забери JSON из `data/exports/` и обработай его как мастер.
9. Загрузи готовый `result.json`.
10. Вызови `/publish_results 1`.

## Команды игрока

```text
/start
/create_character Имя | Пол | Раса | описание | характеристики | заклинание | предмет1, предмет2, предмет3
/profile
/sheet
/list
/inventory
/spells
/allies
/log
/export_sheet
/missions
/join <id миссии>
/action <текст действия>
/my_action
/shop
/rest
/buy <ID товара>
/sell_item <ID предмета>
/sell_pet <имя питомца>
/sell_mount <имя маунта>
/trade @username
/offer_item <ID предмета>
/offer_pet <имя питомца>
/offer_companion <имя спутника>
/offer_mount <имя маунта>
/remove_item <ID предмета>
/remove_pet <имя питомца>
/remove_companion <имя спутника>
/remove_mount <имя маунта>
/offer_gold <дублоны>
/trade_status
/accept_trade
/cancel_trade
```

Ответ игрока на миссию лучше писать в 3-4 абзаца по 2-3 предложения.
Действие должно быть логически связано с миссией и пытаться решить ее конфликт.

Обмен между игроками идет только через бота. Передавать можно предметы, питомцев, спутников, маунтов и дублоны.
Заклинания, уровни и характеристики передавать нельзя.
Предметные ID видны в `/inventory`, имена питомцев, спутников и маунтов - в `/allies` и `/sheet`; сделка завершается только после `/accept_trade` от обоих участников.
Лавка покупает у игроков предметы, питомцев и маунтов: `/shop`, `/buy <ID товара>`, `/sell_item <ID предмета>`, `/sell_pet <имя>`, `/sell_mount <имя>`.
Спутников лавка не покупает.
Каждый новый ход полностью обновляет системный ассортимент лавки; товары игроков, выставленные через продажу, не ротируются автоматически.
Любой актив, механически использованный в миссии, уходит на cooldown на два следующих хода и в это время не добавляет бонус к проверке. Cooldown сохраняется при продаже, выкупе, покупке, обмене и крафте. В лавке команда `/rest` за дублоны сразу восстанавливает все активы героя на перезарядке.

На старте у героя нет питомца, спутника и маунта. Стартовое заклинание и три стартовых предмета имеют 1 уровень.
Описание героя - один краткий художественный абзац; оно помогает писать персональные результаты, но не дает механического бонуса.

Прокачка обычной миссии:

- действие не связано с целью: без роста и без награды;
- слабая попытка, относящаяся к цели: `+1` к уровню, но без награды;
- полноценный вклад в проваленную миссию: `+1` к уровню, но без награды;
- полноценный вклад в успешную миссию: `+2` к уровню и одна награда;
- бонусы не суммируются, герой получает `0`, `+1` или `+2`;
- обычный пул награды: предмет, заклинание, золото или +1 к одной характеристике;
- редкий пул с шансом около 10% открывает питомца/фамильяра, спутника или маунта;
- масштаб обычной награды ограничен личной готовностью героя: `min(ceil(difficulty / 3), рейтинг готовности героя)`;
- количество золота равно `уровень награды * 3`;
- при критическом успехе сильный вклад повышает уровневую награду или золото на 20%;
- награда должна логично соответствовать контексту миссии.
- бот бросает в `reward_roll` редкость, уровень и допустимые типы, а мастер/Codex выбирает конкретный тип и форму по действию героя.

Сложность миссий:

- в ходе должно быть не меньше 3 миссий;
- `difficulty` - общая планка миссии: ее может закрыть один сильный герой или сумма вкладов отряда;
- базовая сила героя: `уровень + релевантная характеристика`, затем примененные активы учитываются с убывающей отдачей `100% / 50% / 25% / 10% / 10%`;
- действие учитывается полностью, если прямо решает цель и объясняет способ; точное использование сцены или координации добавляет `50%` от ядра героя, не умножая всю стоимость лута;
- текст, не относящийся к цели, дает нулевой вклад независимо от силы героя;
- обычная миссия завершается как `success` или `critical_success`, а `ongoing` остается для пройденной промежуточной фазы босса;
- диапазон сложности строится от нижнего и верхнего квартилей готовности активного ростера, а не от обязательной идеальной тройки;
- стандартное число миссий: `max(3, округление вверх(число персонажей / 3))`.

## Команды админа

```text
загрузить арт фото с подписью /turn_art
загрузить turn_seed.yaml файлом в чат с ботом
загрузить turn.yaml файлом в чат с ботом
/status
/export_turn
загрузить result.json файлом в чат с ботом
/publish_results <turn_id>
/chronicle
```

`/export_turn` остается ручным способом закрыть ход раньше дедлайна.
Если `result.json` положить в `data/imports/pending/`, бот сам импортирует и опубликует его.

## Арт хода

Каждый новый `turn.yaml`, который генерирует Codex, должен содержать общий промпт:

```yaml
turn:
  title: "Ход 3: Пепел над каналами"
  deadline: "2026-05-20 22:00"
  art:
    prompt: |
      Wide cinematic fantasy city illustration of Tanellorn...
    caption: "Ход 3: Пепел над каналами"
```

Чтобы картинка ушла игрокам вместе со стартом хода:

1. Сгенерируй арт по `turn.art.prompt`.
2. Отправь картинку боту как фото или image-документ с подписью `/turn_art`.
3. Затем загрузи `turn.yaml`.

Если картинку не загрузить, бот просто откроет ход и пришлет админу арт-промпт текстом.

## Хроника города

После импорта `result.json` бот сохраняет краткую память о миссиях:

- в SQLite-таблицу `city_chronicle`;
- в `data/chronicle/chronicle.json`;
- в `data/chronicle/chronicle.md`.

В хронику попадают `public_summary`, `world_changes`, статус миссии, название хода и название миссии.
При создании workbench для нового seed бот копирует `chronicle.md` рядом с `lore/`, чтобы Codex учитывал прошлые события при генерации новых миссий.

Админ может посмотреть последние записи командой `/chronicle`.

## Файлы

- Пример хода: `examples/turn.sample.yaml`
- Пример seed-файла: `turn_seeds/turn_seed.sample.yaml`
- Пример заметок для обработки: `resolution_notes/resolution_notes.sample.yaml`
- Пример результата: `examples/result.sample.json`
- Главная инструкция для игроков: `docs/player_guide.md`
- Памятка создания персонажа: `docs/character_creation_quick_guide.md`
- Памятка игрового процесса: `docs/gameplay_quick_guide.md`
- Многослойная архитектура режимов: `docs/multimode_architecture.md`
- Инструкция для мастера: `prompts/gm_instructions.md`
- Инструкция генерации миссий: `prompts/mission_generation_instructions.md`
- Инструкция для Codex в рабочем чате: `prompts/codex_operator_guide.md`
- Памятка запуска нового мастерского чата: `docs/master_handoff_guide.md`
- Правила игры: `rules.md`
- Энциклопедия мира: `lore/`
- Хроника города: `data/chronicle/`
- Экспорты ходов: `data/exports/`
- Импорты результатов: `data/imports/`
- Pending-импорты для автообработки: `data/imports/pending/`
- Обработанные pending-импорты: `data/imports/processed/`
- Ошибочные pending-импорты: `data/imports/failed/`
- Бэкапы базы перед импортом: `data/backups/`
- Сохраненные seed-файлы: `data/seeds/`
- Workbench-папки для Codex: `data/workbench/`
- Временный арт следующего хода: `data/art/`

## Как узнать Telegram user ID

Для MVP проще всего написать боту `@userinfobot` в Telegram. Он покажет твой numeric ID.
## Railway

Для облачного запуска на Railway смотри:

[docs/railway_deploy.md](/Users/petrefimov/Documents/New%20project%202/docs/railway_deploy.md)

Если бот запускается в Railway, лучше использовать persistent volume и переменную:

```text
DATA_ROOT=/data
```

Тогда SQLite, экспорт, импорт и хроника будут жить в постоянном хранилище, а не внутри эфемерного контейнера.
